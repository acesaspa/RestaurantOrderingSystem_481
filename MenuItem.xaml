<UserControl x:Class="RestaurantOrderingSystem_481.MenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantOrderingSystem_481"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid Background="#dfed9f" Name="a">

        <Button Name="OrderItem" Background="{x:Null}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <!--"Auto" adjusts the height based on how much text is in Name and Description-->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <!--the '*' allows the width ratio with respect to the DesignWidth to remain the same, even if the window size changes-->
                    <ColumnDefinition Width="200*"/>
                    <ColumnDefinition Width="300*"/>
                    <ColumnDefinition Width="100*"/>
                </Grid.ColumnDefinitions>
                <Image HorizontalAlignment="Left" VerticalAlignment="Top" Source="Resources/stockSalad.jpg" Grid.RowSpan="2" Name="ItemImage"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Name="ItemName" Text="Item Name" TextWrapping="Wrap"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Name="ItemDescription" Text="Item Description" TextWrapping="Wrap"/>
                <TextBlock Grid.Column="2" Grid.Row="0" Grid.ColumnSpan="2" Name="ItemPrice" Text="Item Price" HorizontalAlignment="Right" TextWrapping="Wrap"/>
            </Grid>

        </Button>
    </Grid>


</UserControl>
